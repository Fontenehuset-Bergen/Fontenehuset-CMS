# todo-list
- hvordan skal vi bestemme hvilke data som skal hentes? baserer vi det kun på date>now() eller ligger vi til logikk for å finne en unique identifier? har trello i det hele tatt uuid?
- sitt data inn i en queue, verifiser dups for hver iterasjon, eller group?
- queue må logge hver entry den behandler, viktig at hver operasjon kan gi beskjed hvis den feiler.
- Hvis N feil skjer, spør bruker om de vill avbryte?
- hvordan behandle feilmeldinger?
- Lagre log?